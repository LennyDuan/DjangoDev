<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenny.studyresearchapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:name=".network.BackendVolley"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:logo="@drawable/icons8go"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AccountActivities"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DiaryActivities"
            android:label="@string/activity_diary"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".QuestionnaireActivities"
            android:label="@string/activity_questionnaire"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DiaryDetail"
            android:label="Diary Detail"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".BasicInfo"
            android:label="Basic Info"
            android:theme="@style/AppTheme">
        </activity>
    </application>

</manifest>